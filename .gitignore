/node_modules
/.next
/out
/build
.env*
# ...but the template carries no secrets and must be committed
!.env.local.example
.DS_Store
*.log
.vercel

# npm run check builds here so it never disturbs the dev server
.next-check
